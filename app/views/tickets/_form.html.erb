<div class="textarea">
<%= form_for([@project, @ticket]) do |f| %>
<p>
<%= render 'shared/error_messages', object: f.object %>
<%= f.label(:title) %>
<%= f.text_field(:title) %>

<%= f.label(:description) %>
<%= f.text_area(:description) %>
</p>
<%= f.submit %>
<% end %>
</div>